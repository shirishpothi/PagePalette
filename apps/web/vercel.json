{
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "react-router",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ],
  "functions": {
    "api/order.js": {
      "runtime": "@vercel/node@3.0.0"
    }
  }
}